# 连续影像 FrameCurrent｜历史验收报告

> 这是 2026-09-01 的历史付费与媒体链证据快照。当前 FrameCurrent 1.6.1 产品验证状态、五个预设频道、自定义频道、不限时模式与 71 项离线测试请以 [`docs/VERIFICATION.md`](docs/VERIFICATION.md) 为准；本文件中的“39 / 39”和“四种预设”仅描述当时版本。

> 1.6.0 的每幕计时在本次历史付费验收之后新增，不属于 2026-09-01 的付费证据。它优先采用 fal 返回的 GPU 核心推理指标，缺失时依次降级为 fal processing 与本机 result-ready 观测值。GPU 核心计时不含排队、编码、下载和本地处理；任何来源都不构成 MiniMax 或 fal 的官方速度结论。

验收日期：2026-09-01
环境：macOS、Python 3.9、Swift 6.3、AVFoundation、`avmediainfo`

## 结论

**产品现支持 10 秒至 30 分钟自由时长，以及 9:16 竖屏和 16:9 横屏；此前完成的真实 H3 Max 六幕、70 秒竖屏连续性验收仍作为历史证据保留。**

界面不再用固定时长卡限制用户。用户可以自由输入 10 秒至 30 分钟的总时长，软件自动规划 5–15 秒合法片段并精确合计到目标时长。演示模式和录屏功能未包含在交付版中；生成过程可以提前预览，完成后自动合并并提供完整 MP4 播放与下载。

历史真实付费验收完成 1 段文生视频和 5 段末帧图生视频：请求总长 70 秒，六段均为 768×1344、24 fps、H.264 视频和 AAC 音频。五个跨幕接缝的内部诊断相似度为 0.9900–0.9925；最终母版严格为 70.000 秒，并已在 QuickTime 完整播放至 1:10。此前没有付费生成完整 5 分钟，软件的 300 秒精确合并链使用离线媒体完成压力验证。**本轮自由时长与横竖屏升级未再次发起任何 fal 付费生成任务。**

## 已实现功能

- fal Key 无费用价格接口鉴权；明确提示这不等于余额或生成权限已经验证。
- 真实 H3 Max 文生视频 → 末帧提取 → 图生视频续写链。
- 总时长可在 10 秒至 30 分钟之间自由输入，并自动组合 5–15 秒合法片段，精确合计到目标时长。
- 9:16 竖屏与 16:9 横屏两种成片比例。
- 日系手绘奇幻、电影级科幻、高能棚内综艺、旅行电影航拍四种高差异预设；每种都有独立主体、场景、动作、镜头、排除项、后端基础提示词和逐幕事件。
- 文生视频首幕直接向 H3 Max 传入用户选择的 `aspect_ratio`；图生视频没有独立比例字段，沿用参考图或上一幕末帧的比例。
- 至少两幕准备好后，双播放器开始“边生成、边预览”。
- 缓冲不足时保留上一幕末帧，不显示黑屏或伪造进度。
- 每幕显示真实生成状态、已生成视频时长、缓冲、剩余时间与保守预计费用；视频内容时长与生成计时是两个不同指标。
- 480P / 768P 标准价总预算检查，以及每次提交前的二次硬保护。
- fal 队列地址和媒体下载地址仅接受官方 HTTPS 域名及标准 443 端口。
- 每幕验证时长、所选横竖屏比例、H.264、AAC 和媒体可读性，并提取首尾帧。
- H3 Max 源片允许最多 5 帧生成余量；合并时将每幕视频与音频同步裁到计划时长，保证最终成片长度准确。
- 全部完成后统一重编码为所选比例的 H.264 MP4；源片有音频时合并为 AAC 单音轨。
- 成片记录字节数和 SHA-256；软件重启后重新验证，再开放下载。
- API Key 只存在于页面当前输入和 Python 进程内存，任务启动后页面清空，任务结束后后端清空。
- 公开任务状态不包含 Key、参考图、完整提示词、请求 ID、本机路径或底层媒体回执。
- 队列提交带 `X-Fal-Store-IO: 0`，阻止 fal 持久化保存提示词和末帧 Data URI；本机仍立即下载媒体文件。
- 用户停止时对当前 fal 任务发送一次尽力取消；提交 POST 永不自动重试，只有状态和结果 GET 会在瞬时错误时进行有界重试。

## 自动化与离线媒体验收

执行：

```bash
/usr/bin/python3 -m unittest discover -s tests -v
```

结果：**39 / 39 PASS**。测试不提交任何 fal 生成请求；启动线程、Key 验证和队列网络均使用本地替身。

覆盖：

- 10 秒至 30 分钟任意整数秒的前后端边界。
- 不同总时长和 5–15 秒单片偏好均能生成精确计划。
- 9:16、16:9 合法比例以及非法比例拒绝。
- 调用方传入 `mode/mock` 无法改变真实生成路径。
- 文生视频按所选比例提交，图生视频沿用输入图像比例。
- 严格的付费确认、总预算和逐幕预算边界。
- 敏感字段递归脱敏，包括远端错误文本侧信道。
- fal queue/media 域名、协议、端口和目录穿越防护。
- `COMPLETED` 状态及结果中的 `error/error_type` 不会被误判为成功。
- 提交 POST 不重试、GET 瞬时错误有界重试、停止只取消一次，以及取消地址不会写入公开清单。
- `X-Fal-Store-IO: 0` 隐私请求头。
- 实时预览、最终文件存在和下载状态。
- 余额不足导致请求未受理时，提交时长和预计费用保持为 0。

媒体压力测试使用不随产品交付的本地夹具：

| 项目 | 结果 |
|---|---:|
| 输入 | 20 × 15.167 秒竖屏片段 |
| 合并输出时长 | 300.000 秒 |
| 输出尺寸 | 540 × 960 |
| 视频 | H.264，24 fps |
| `avmediainfo` | 0 error |
| 后端 `validate_media` | PASS |

这组夹具原始总长约 303.333 秒，合并器逐段按计划裁切后精确输出 300.000 秒，证明真实 H3 每幕多出的少量帧不会累积到最终成片。

横屏媒体链也使用离线夹具完成合并与比例验证；它验证 16:9 处理能力，但不代表本轮新生成了真实 H3 Max 横屏素材。

真实 H3 连续母版验收：六段源片共约 70.670 秒，按每幕请求时长同步裁切后输出 70.000 秒、768×1344、24 fps、H.264 + AAC，`avmediainfo` 0 error。母版、第一幕原片、首尾帧和独立记录保存在项目所有者本机的 `real-test/`，不随公开仓库分发。

另完成两幕离线端到端替身测试：任务状态 COMPLETE、30 秒完整 MP4、密钥在结束后清空、公开 JSON 不含模拟请求 ID、文件 SHA-256 校验通过。

真实 H3 Max 原生 AAC 已完成六幕下载、分轨识别和合并验证；六段音轨按 0、5、10、25、40、55 秒排入 70 秒母版。完整播放器可正常播放并自然到达结尾。结构检查证明音轨连续存在，但不把它表述为人工听感或无音频接缝保证。

## 浏览器验收

- 桌面端和 390 × 844 手机视口均完成视觉检查。
- 四种成片类型具有独立色彩与类型标签；逐项切换均能正确更新主体、场景、动作、镜头、排除项和预览区主题。
- 用户手动改写字段后再切换类型，该字段保持不变，其他仍使用默认值的字段正常更新。
- 自由时长输入可覆盖 10 秒至 30 分钟，并对越界值给出阻止提示。
- 9:16 与 16:9 选择会同步更新创作表单、预览画布和参考图提示。
- 未验证 Key、未勾选费用确认或预算不足时，开始按钮保持禁用。
- 无效 Key 显示公共错误文案，不暴露远端响应。
- 首页和完整视频播放器的浏览器控制台均为 0 条 error/warning。
- 完整播放器找不到任务时进入清晰错误状态，不出现空白页。
- HTTP 下载测试同时覆盖普通 200、Range 206、后缀 Range、非法 Range 416 和 `Content-Disposition: attachment`。

## 历史真实付费测试状态

计划：

1. 5 秒文生视频。
2. 5 秒末帧续写。
3. 上述两幕通过后，再续写 4 × 15 秒。
4. 合并为 70 秒验收母版。

保守标准价预算：70 秒 × $0.08/秒 = **$5.60**，不自动重试。

实际结果：

- 六个任务全部被 fal 队列受理并完成：5 秒 T2V、5 秒 I2V 和 4 × 15 秒 I2V；提交 POST 均只发送一次，没有自动重试。
- 六段源片实际时长为 5.167、5.167、15.084、15.084、15.084、15.084 秒。每段允许少量帧级余量，最终把画面与音频同步裁到 70.000 秒。
- 五个接缝的内部诊断相似度依次为 0.9922、0.9925、0.9924、0.9900、0.9902，均通过明显跳变检查。
- 完整观看确认白色纸船、单个暖光灯笼、夜色水城和运动方向在六幕中持续存在，没有硬切、主体替换或消失。纸船折痕、比例和远近仍有轻微渐进漂移，因此不宣称五分钟像素级不变。
- 母版为 70.000 秒、768×1344、24 fps、H.264 + AAC，65,954,783 字节，SHA-256 为 `c7d4a241e4b25c3c2555abafb271823b0346920ede6fdbbee29f948fcd226d62`；QuickTime 完整播放至 1:10。
- 按 768P 标准保护价计算，最高可能费用为 **$5.60**，没有超过本次 $5–8 上限。普通生成 Key 无账单权限，精确扣费仍以 fal Dashboard 为准。

完整的付费验收记录和媒体证据由项目所有者离线保管，未包含在公开仓库中。

## 当前内容方向

当前四种预设覆盖原创日系手绘奇幻、电影级科幻、高能棚内综艺和旅行电影航拍。它们的视觉语言完全不同，但都采用“一个明确主体、一条连续物理路线、一个稳定镜头逻辑”，在视觉冲击力与长链连续性之间取得平衡。历史白色纸船测试仍只作为已完成的真实竖屏链路证据，不再代表当前默认创作方向。

连续生成的长素材应当作为下一期作品的素材母版，而不是直接发布。建议从中选择 15–45 秒最有变化的段落，配合后期旁白、字幕、品牌信息和统一音乐放入抖音或视频号作品。第一次长链避免写实真人、多人物对白、手部操作、快速打斗和画内文字。

## 验收边界

历史真实连续性验收在获准的 $5–8 预算内完成到 70 秒，并覆盖文生首幕、末帧续写、六幕下载、五个接缝、音视频裁切、合并、完整播放和下载文件校验。它证明软件的真实竖屏链路与当时画面方向可连续工作，但不等同于已经付费生成并人工观看了完整 5 分钟，也不等同于本轮已经付费生成横屏素材。

5 分钟历史流程的媒体处理已通过 20 × 15.167 秒离线压力片精确输出 300.000 秒；当前版本还通过自动化测试覆盖 10 秒至 30 分钟的自由时长计划与两种比例。若以后单独进行完整长时段真实压力测试，需要新的费用授权，并应按所选清晰度和总秒数重新计算预算；这不是本轮已经执行的付费验收。
